import 'package:flutter_test/flutter_test.dart';

void main() {
  group('UserKey model', () {
    test('requires userId and publicKey', () {
      fail('not implemented');
    });

    test('retiredAt is nullable (null means currently active)', () {
      fail('not implemented');
    });
  });
}
